package kotlin.reflect.jvm.internal.impl.renderer;

import com.facebook.react.bridge.PromiseImpl;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Set;
import org.apache.pdfbox.pdfparser.BaseParser;
import sfs2x.client.requests.game.JoinRoomInvitationRequest;

public class KeywordStringsGenerated {
    public static final Set<String> KEYWORDS = new HashSet(Arrays.asList(new String[]{"package", JoinRoomInvitationRequest.KEY_AS_SPECT, "typealias", PromiseImpl.STACK_FRAME_KEY_CLASS, "this", "super", "val", "var", "fun", "for", "null", BaseParser.TRUE, BaseParser.FALSE, "is", "in", "throw", "return", "break", "continue", "object", "if", "try", "else", "while", "do", "when", "interface", "typeof"}));
}
